# Deferred reciprocals

Cross-site edges that point at target slugs not yet shipped on the target repo. These do **not** fail the audit — they're a running log of reciprocals to add when those topics publish.

Regenerated by `pnpm audit:cross-site` (or directly via `node scripts/audit-cross-site-links.mjs`).

## When `formalcalculus/continuity` ships

Add the reciprocal pointer back to:

- **formalStatistics/empirical-processes** declares `formalcalculusPrereqs` → `continuity` (prereqs)
  - Reciprocal field on the new topic: `formalstatisticsConnections`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _§32.7's Hadamard differentiability is the uniform-convergence analog of pointwise differentiability; §32.7 Rem 17 cites continuity on function spaces directly._

## When `formalcalculus/convex-optimization` ships

Add the reciprocal pointer back to:

- **formalStatistics/regularization-and-penalized-estimation** declares `formalcalculusPrereqs` → `convex-optimization` (prereqs)
  - Reciprocal field on the new topic: `formalstatisticsConnections`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _§23.3 Thm 2 (lasso KKT) and §23.4 Thm 6 (coord-descent convergence) both rely on subgradient calculus and the optimality conditions for convex non-smooth functions. The subgradient of $|\beta_j|$ —…_

## When `formalcalculus/differentiation` ships

Add the reciprocal pointer back to:

- **formalStatistics/central-limit-theorem** declares `formalcalculusPrereqs` → `differentiation` (prereqs)
  - Reciprocal field on the new topic: `formalstatisticsConnections`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Taylor's theorem with remainder provides the rigorous bound on the remainder term in the MGF expansion. The delta method requires differentiability of the transformation g._
- **formalStatistics/confidence-intervals-and-duality** declares `formalcalculusPrereqs` → `differentiation` (prereqs)
  - Reciprocal field on the new topic: `formalstatisticsConnections`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _The profile likelihood ℓ_P(θ) = sup_ψ ℓ(θ, ψ) is a composition of an implicit function (ψ̂(θ) solves the nuisance score equation) and the joint likelihood. Differentiation under the sup is the Dans…_
- **formalStatistics/hypothesis-testing** declares `formalcalculusPrereqs` → `differentiation` (prereqs)
  - Reciprocal field on the new topic: `formalstatisticsConnections`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _The power function $\beta(\theta)$ is differentiated to characterize most-powerful tests (previewed in §17.4, full treatment in Topic 18). The score test uses $\partial_\theta \ell(\theta)$ directly._
- **formalStatistics/large-deviations** declares `formalcalculusPrereqs` → `differentiation` (prereqs)
  - Reciprocal field on the new topic: `formalstatisticsConnections`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _First-order optimization of the Chernoff bound over t (the tilting parameter). The Legendre transform I(x) = sup_t(tx − log M(t)) is a convex conjugate, requiring differentiation to evaluate. Taylo…_
- **formalStatistics/likelihood-ratio-tests-and-np** declares `formalcalculusPrereqs` → `differentiation` (prereqs)
  - Reciprocal field on the new topic: `formalstatisticsConnections`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _The Wilks proof's Step 2 is Taylor's theorem with remainder, applied to the log-likelihood $\ell(\theta)$ around the MLE $\hat\theta_n$. Step 3 uses the first-order condition $\ell'(\hat\theta_n) =…_
- **formalStatistics/maximum-likelihood** declares `formalcalculusPrereqs` → `differentiation` (prereqs)
  - Reciprocal field on the new topic: `formalstatisticsConnections`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _The score function is ∂/∂θ log L(θ), and the asymptotic-normality proof Taylor-expands the score around θ₀. Newton-Raphson requires the second derivative ∂²/∂θ² log L(θ) = −observed Fisher informat…_
- **formalStatistics/method-of-moments** declares `formalcalculusPrereqs` → `differentiation` (prereqs)
  - Reciprocal field on the new topic: `formalstatisticsConnections`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _The delta method uses the Jacobian of the moment-to-parameter map; the M-estimator sandwich formula uses partial derivatives of ψ(x; θ) with respect to θ._
- **formalStatistics/point-estimation** declares `formalcalculusPrereqs` → `differentiation` (prereqs)
  - Reciprocal field on the new topic: `formalstatisticsConnections`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _The score function is the derivative ∂/∂θ log f(X; θ), and Fisher information is E[(∂/∂θ log f)²]. The Cramér-Rao proof requires interchange of differentiation and integration under regularity cond…_
- **formalStatistics/sufficient-statistics** declares `formalcalculusPrereqs` → `differentiation` (prereqs)
  - Reciprocal field on the new topic: `formalstatisticsConnections`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _The PKD proof cross-differentiates the log-factorization in the data argument and the parameter, deriving the separable equation that characterizes the exponential-family form._

## When `formalcalculus/eigenvalues-eigenvectors` ships

Add the reciprocal pointer back to:

- **formalStatistics/multivariate-distributions** declares `formalcalculusPrereqs` → `eigenvalues-eigenvectors` (prereqs)
  - Reciprocal field on the new topic: `formalstatisticsConnections`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _The spectral decomposition Σ = QΛQ^T gives the principal axes of the MVN distribution. Eigenvalues are the variances along principal axes. This is the mathematical core of PCA._

## When `formalcalculus/induction` ships

Add the reciprocal pointer back to:

- **formalStatistics/multiple-testing-and-false-discovery** declares `formalcalculusPrereqs` → `induction` (prereqs)
  - Reciprocal field on the new topic: `formalstatisticsConnections`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Proof 3 (Holm) is an induction on the number of rejections $r$. The base case $r = 1$ reduces to Bonferroni; the inductive step uses the threshold $\alpha/(m - r + 1)$ on the remaining $m - r + 1$ …_

## When `formalcalculus/inner-products-and-projections` ships

Add the reciprocal pointer back to:

- **formalStatistics/linear-regression** declares `formalcalculusPrereqs` → `inner-products-and-projections` (prereqs)
  - Reciprocal field on the new topic: `formalstatisticsConnections`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Theorem 1 (OLS-as-orthogonal-projection) is an application of the projection theorem in finite-dimensional inner-product spaces: the unique closest point in $\text{col}(\mathbf{X})$ to $\mathbf{y}$…_

## When `formalcalculus/integration` ships

Add the reciprocal pointer back to:

- **formalStatistics/bayesian-computation-and-mcmc** declares `formalcalculusPrereqs` → `integration` (prereqs)
  - Reciprocal field on the new topic: `formalstatisticsConnections`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Ergodic averages (§26.7) are integrals against the invariant measure π, estimated by Cesàro time-averaging. The Kac formula (renewal interpretation in Proof 4 Step 3) is a standard integration-by-p…_
- **formalStatistics/confidence-intervals-and-duality** declares `formalcalculusPrereqs` → `integration` (prereqs)
  - Reciprocal field on the new topic: `formalstatisticsConnections`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Coverage probability P_θ(θ ∈ C(X)) is an integral of the sampling density over the event {θ ∈ C(X)}. The coverage diagnostic in §19.8 computes this integral numerically for binomial p._
- **formalStatistics/hypothesis-testing** declares `formalcalculusPrereqs` → `integration` (prereqs)
  - Reciprocal field on the new topic: `formalstatisticsConnections`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Rejection-region probabilities under both $H_0$ and $H_\theta$ are integrals of the sampling density over $R$. Power $\beta(\theta) = \int_R f(x; \theta)\,dx$ is a calculus-of-variations object in …_
- **formalStatistics/large-deviations** declares `formalcalculusPrereqs` → `integration` (prereqs)
  - Reciprocal field on the new topic: `formalstatisticsConnections`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Expectation integrals in MGF computations. The Cramér rate function involves a supremum over an integral expression._
- **formalStatistics/law-of-large-numbers** declares `formalcalculusPrereqs` → `integration` (prereqs)
  - Reciprocal field on the new topic: `formalstatisticsConnections`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _The dominated convergence theorem is the key tool in the truncation argument: E[X₁ 1(|X₁| ≤ n)] → E[X₁] as n → ∞, and E[X₁² 1(|X₁| ≤ n)]/n → 0. Both limits are justified by DCT._
- **formalStatistics/likelihood-ratio-tests-and-np** declares `formalcalculusPrereqs` → `integration` (prereqs)
  - Reciprocal field on the new topic: `formalstatisticsConnections`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _The Neyman-Pearson proof integrates the indicator-function inequality $(\varphi^* - \varphi)(f_1 - k f_0) \ge 0$ over the sample space to convert a pointwise statement into an expectation inequalit…_
- **formalStatistics/maximum-likelihood** declares `formalcalculusPrereqs` → `integration` (prereqs)
  - Reciprocal field on the new topic: `formalstatisticsConnections`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Regularity conditions for the MLE require interchange of derivative and integral — the same conditions as the CRLB in Topic 13._
- **formalStatistics/modes-of-convergence** declares `formalcalculusPrereqs` → `integration` (prereqs)
  - Reciprocal field on the new topic: `formalstatisticsConnections`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Lp convergence is defined via the integral norm E[|Xn − X|^p] = ∫|Xn − X|^p dP. The dominated convergence theorem is used to interchange limits and integrals when proving that Lp implies convergenc…_
- **formalStatistics/point-estimation** declares `formalcalculusPrereqs` → `integration` (prereqs)
  - Reciprocal field on the new topic: `formalstatisticsConnections`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _The regularity conditions for the Cramér-Rao bound require ∂/∂θ ∫ f(x;θ) dx = ∫ (∂/∂θ) f(x;θ) dx — interchange of derivative and integral, justified by dominated convergence._
- **formalStatistics/sufficient-statistics** declares `formalcalculusPrereqs` → `integration` (prereqs)
  - Reciprocal field on the new topic: `formalstatisticsConnections`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _The factorization theorem and Rao-Blackwell proofs use joint-density integration and the interchange of integration with conditional expectation (Fubini). The PKD proof integrates a separable funct…_

## When `formalcalculus/integration-by-parts` ships

Add the reciprocal pointer back to:

- **formalStatistics/continuous-distributions** declares `formalcalculusPrereqs` → `integration-by-parts` (prereqs)
  - Reciprocal field on the new topic: `formalstatisticsConnections`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Computing E[X] for the Exponential and Gamma distributions requires integration by parts. The Gamma function recursion Γ(α+1) = αΓ(α) is proved by integration by parts._
- **formalStatistics/expectation-moments** declares `formalcalculusPrereqs` → `integration-by-parts` (prereqs)
  - Reciprocal field on the new topic: `formalstatisticsConnections`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Computing E[X] for the Exponential and Gamma distributions requires integration by parts. The formula ∫x·λe^{−λx}dx = 1/λ is a standard application._

## When `formalcalculus/linear-algebra` ships

Add the reciprocal pointer back to:

- **formalStatistics/method-of-moments** declares `formalcalculusPrereqs` → `linear-algebra` (prereqs)
  - Reciprocal field on the new topic: `formalstatisticsConnections`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _The sandwich variance A⁻¹BA⁻¹ᵀ is a matrix product; positive-definiteness of the covariance of sample moments is a quadratic-form argument._

## When `formalcalculus/matrix-calculus` ships

Add the reciprocal pointer back to:

- **formalStatistics/generalized-linear-models** declares `formalcalculusPrereqs` → `matrix-calculus` (prereqs)
  - Reciprocal field on the new topic: `formalstatisticsConnections`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _§22.3 Thm 1's Hessian $\mathcal{H}(\boldsymbol\beta) = -\mathbf{X}^\top\mathbf{W}\mathbf{X}$ uses the standard quadratic-form derivative $\partial(\mathbf{x}^\top\mathbf{A}\mathbf{x})/\partial\math…_
- **formalStatistics/model-selection-and-information-criteria** declares `formalcalculusPrereqs` → `matrix-calculus` (prereqs)
  - Reciprocal field on the new topic: `formalstatisticsConnections`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _§24.5 Stone's hat-matrix identity uses the trace cyclicity property; §24.8 derives ridge effective DOF $\sum_j d_j^2/(d_j^2+\lambda)$ in SVD coordinates. Both rely on standard matrix-calculus ident…_
- **formalStatistics/regularization-and-penalized-estimation** declares `formalcalculusPrereqs` → `matrix-calculus` (prereqs)
  - Reciprocal field on the new topic: `formalstatisticsConnections`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Ridge's closed-form $(\mathbf{X}^\top\mathbf{X} + \lambda\mathbf{I})^{-1}\mathbf{X}^\top\mathbf{y}$ uses the standard quadratic-form derivative $\partial(\mathbf{x}^\top\mathbf{A}\mathbf{x})/\parti…_

## When `formalcalculus/measure-theoretic-probability` ships

Add the reciprocal pointer back to:

- **formalML/extreme-value-theory** declares `formalcalculusPrereqs` → `measure-theoretic-probability` (prereqs)
  - Reciprocal field on the new topic: `formalmlConnections`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _The weak-convergence framework that all of §§2–3 lean on lives there: convergence-in-distribution as weak convergence of probability measures, the Portmanteau theorem (used explicitly in the Khintc…_

## When `formalcalculus/multivariable-calculus` ships

Add the reciprocal pointer back to:

- **formalStatistics/bayesian-computation-and-mcmc** declares `formalcalculusPrereqs` → `multivariable-calculus` (prereqs)
  - Reciprocal field on the new topic: `formalstatisticsConnections`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _§26.4's leapfrog integrator uses ∇U(θ) = −∇ log π(θ | y) at every half-step. Volume preservation (Proof 3) uses the Jacobian determinant._
- **formalStatistics/bayesian-foundations-and-prior-selection** declares `formalcalculusPrereqs` → `multivariable-calculus` (prereqs)
  - Reciprocal field on the new topic: `formalstatisticsConnections`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _§25.8 Proof 3 (BvM sketch) Taylor-expands the log-posterior in a neighborhood of $\hat\theta_{\text{MLE}}$; the leading Gaussian term is extracted via standard Taylor-with-remainder. The Laplace ap…_
- **formalStatistics/generalized-linear-models** declares `formalcalculusPrereqs` → `multivariable-calculus` (prereqs)
  - Reciprocal field on the new topic: `formalstatisticsConnections`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _The score $\nabla_{\boldsymbol\beta}\ell$ and Hessian $\nabla^2_{\boldsymbol\beta}\ell$ are vector- and matrix-valued partial derivatives over $\boldsymbol\beta \in \mathbb{R}^{p+1}$. §22.3 Proofs …_
- **formalStatistics/model-selection-and-information-criteria** declares `formalcalculusPrereqs` → `multivariable-calculus` (prereqs)
  - Reciprocal field on the new topic: `formalstatisticsConnections`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Akaike's proof (§24.3 Proof 1) Taylor-expands $\ell(\theta)$ around $\theta_0$ in $p+1$ dimensions; the BIC-Laplace proof (§24.4 Proof 2) does multivariate Laplace with a Hessian determinant. Both …_
- **formalStatistics/regularization-and-penalized-estimation** declares `formalcalculusPrereqs` → `multivariable-calculus` (prereqs)
  - Reciprocal field on the new topic: `formalstatisticsConnections`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _MSE decomposition (§23.3 Proof 1) and the SVD-coordinate rotation $\boldsymbol\gamma = \mathbf{V}^\top\boldsymbol\beta$ require multivariate Taylor expansion and the rotational invariance of $\|\cd…_

## When `formalcalculus/multivariable-integration` ships

Add the reciprocal pointer back to:

- **formalStatistics/bayesian-foundations-and-prior-selection** declares `formalcalculusPrereqs` → `multivariable-integration` (prereqs)
  - Reciprocal field on the new topic: `formalstatisticsConnections`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _The marginal likelihood $m(\mathbf{y}) = \int L(\theta)\pi(\theta)\,d\theta$ and posterior predictive $p(\tilde y \mid \mathbf{y}) = \int p(\tilde y \mid \theta)p(\theta \mid \mathbf{y})\,d\theta$ …_
- **formalStatistics/bayesian-model-comparison-and-bma** declares `formalcalculusPrereqs` → `multivariable-integration` (prereqs)
  - Reciprocal field on the new topic: `formalstatisticsConnections`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _The Laplace expansion of $m(\mathbf{y}) = \int L(\theta)\pi(\theta)\,d\theta$ uses multivariate Gaussian integration over a neighborhood of the posterior mode. §27.6 Proof 2 cites this mechanism di…_
- **formalStatistics/hierarchical-bayes-and-partial-pooling** declares `formalcalculusPrereqs` → `multivariable-integration` (prereqs)
  - Reciprocal field on the new topic: `formalstatisticsConnections`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _The Jacobian determinant in §28.9 Thm 7's non-centered volume-preservation proof uses multivariate change of variables on the block-triangular map $(\tilde\theta, \mu, \tau) \mapsto (\theta, \mu, \…_
- **formalStatistics/kernel-density-estimation** declares `formalcalculusPrereqs` → `multivariable-integration` (prereqs)
  - Reciprocal field on the new topic: `formalstatisticsConnections`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _The MISE integral $\int \int (\hat f_h(x) - f(x))^2 f(y)\, dy\, dx$ requires comfort with interchanging expectation and integration — Fubini–Tonelli from multivariable integration._
- **formalStatistics/multivariate-distributions** declares `formalcalculusPrereqs` → `multivariable-integration` (prereqs)
  - Reciprocal field on the new topic: `formalstatisticsConnections`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Marginalizing a p-dimensional joint density requires iterated integrals over R^{p-q}. The general framework in §2 assumes Fubini's theorem for exchanging the order of integration._
- **formalStatistics/order-statistics-and-quantiles** declares `formalcalculusPrereqs` → `multivariable-integration` (prereqs)
  - Reciprocal field on the new topic: `formalstatisticsConnections`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _The joint-density derivation in §29.2 is a Jacobian change-of-variables calculation over the ordered simplex. Rényi's proof (§29.3) uses the same machinery on the spacings map._
- **formalML/statistical-depth** declares `formalcalculusPrereqs` → `multivariable-integration` (prereqs)
  - Reciprocal field on the new topic: `formalmlConnections`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Halfspace mass $F(H)$ and functional-band mass $F(B)$ are concrete multivariable integrals; the §§2–5 derivations and Proposition 1's convexity proof both sit in this ambient setting._

## When `formalcalculus/ordinary-differential-equations` ships

Add the reciprocal pointer back to:

- **formalStatistics/bayesian-computation-and-mcmc** declares `formalcalculusPrereqs` → `ordinary-differential-equations` (prereqs)
  - Reciprocal field on the new topic: `formalstatisticsConnections`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Hamilton's equations (ẋ = ∂H/∂p, ṗ = −∂H/∂q) form an ODE system; §26.4 Def 4 and Rem 11 draw on ODE theory to contrast exact flow with the symplectic leapfrog approximation._

## When `formalcalculus/probability-and-union-bound` ships

Add the reciprocal pointer back to:

- **formalStatistics/multiple-testing-and-false-discovery** declares `formalcalculusPrereqs` → `probability-and-union-bound` (prereqs)
  - Reciprocal field on the new topic: `formalstatisticsConnections`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Proof 1 (Bonferroni) is the union bound applied once. The union bound on the event $\{V \ge 1\} = \bigcup_{i \in H_0} \{p_i \le \alpha/m\}$ is the only analytical tool Bonferroni requires._

## When `formalcalculus/quadratic-forms` ships

Add the reciprocal pointer back to:

- **formalStatistics/linear-regression** declares `formalcalculusPrereqs` → `quadratic-forms` (prereqs)
  - Reciprocal field on the new topic: `formalstatisticsConnections`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _§21.7 characterizes the hat matrix $\mathbf{H}$ as an orthogonal projector — idempotent, symmetric, with $\text{rank}(\mathbf{H}) = \text{tr}(\mathbf{H}) = p + 1$. The spectral decomposition of a s…_

## When `formalcalculus/taylor-series-and-approximation` ships

Add the reciprocal pointer back to:

- **formalStatistics/bootstrap** declares `formalcalculusPrereqs` → `taylor-series-and-approximation` (prereqs)
  - Reciprocal field on the new topic: `formalstatisticsConnections`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _The Edgeworth expansion in §31.5 Theorem 5 is a Taylor-style series for a CDF around the standard-normal CDF, with coefficients driven by the cumulants of the statistic._
- **formalStatistics/kernel-density-estimation** declares `formalcalculusPrereqs` → `taylor-series-and-approximation` (prereqs)
  - Reciprocal field on the new topic: `formalstatisticsConnections`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _§30.4's bias derivation Taylor-expands $f$ around $x$ to second order; §30.6's optimization Taylor-expands AMISE around $h^\ast$. Both reduce to first- and second-order conditions on standard Taylo…_
- **formalStatistics/order-statistics-and-quantiles** declares `formalcalculusPrereqs` → `taylor-series-and-approximation` (prereqs)
  - Reciprocal field on the new topic: `formalstatisticsConnections`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Bahadur's proof (§29.6) Taylor-expands $F$ around $\xi_p$ and controls the remainder — the first-order term is load-bearing, the second-order term is $O(1/n)$._

## When `formalcalculus/vector-spaces` ships

Add the reciprocal pointer back to:

- **formalStatistics/linear-regression** declares `formalcalculusPrereqs` → `vector-spaces` (prereqs)
  - Reciprocal field on the new topic: `formalstatisticsConnections`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _§21.4's matrix-form pivot rests on linear algebra essentials: $\mathbf{X}$ is $n \times (p+1)$, $\text{col}(\mathbf{X}) \subseteq \mathbb{R}^n$ is a $(p+1)$-dimensional subspace under full rank, an…_

## When `formalml/ab-testing` ships

Add the reciprocal pointer back to:

- **formalStatistics/bootstrap** declares `formalmlConnections` → `ab-testing` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _§31.6's bootstrap hypothesis test is the distribution-free alternative to the parametric $t$-test for A/B significance — robust to non-Normal metric distributions that plague click-through and conv…_

## When `formalml/ab-testing-platforms` ships

Add the reciprocal pointer back to:

- **formalStatistics/confidence-intervals-and-duality** declares `formalmlConnections` → `ab-testing-platforms` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Every deployed A/B-test platform reports a confidence interval on the treatment effect alongside the p-value. The Wilson interval is the industry default for rare-event conversion metrics (§19.5); …_
- **formalStatistics/hypothesis-testing** declares `formalmlConnections` → `ab-testing-platforms` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _A/B testing is hypothesis testing applied at platform scale. The null 'Design A and Design B have the same conversion rate' is a two-sample proportion test; sample-size calculations are §17.4 power…_
- **formalStatistics/likelihood-ratio-tests-and-np** declares `formalmlConnections` → `ab-testing-platforms` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _A/B platforms default to Wald z-tests, but as §18.8 shows, for boundary-rare events (conversion rates near 0 or 1) the Wald test's reparameterization dependence can flip the rejection decision. Mod…_

## When `formalml/always-valid-inference` ships

Add the reciprocal pointer back to:

- **formalStatistics/multiple-testing-and-false-discovery** declares `formalmlConnections` → `always-valid-inference` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _The single-test framework of §20.3 assumes the $m$ hypotheses and the stopping rule are fixed in advance. Always-valid confidence sequences (Howard et al. 2021) and e-processes (Vovk 2019, Grünwald…_

## When `formalml/bayesian-inference` ships

Add the reciprocal pointer back to:

- **formalStatistics/conditional-probability** declares `formalmlConnections` → `bayesian-inference` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _The posterior distribution P(θ|data) ∝ P(data|θ)P(θ) is Bayes' theorem applied to parameter spaces. Every MCMC algorithm, every variational inference method targets this posterior._
- **formalStatistics/continuous-distributions** declares `formalmlConnections` → `bayesian-inference` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Beta-Bernoulli, Gamma-Poisson, and Normal-Normal are the three canonical conjugate pairs used in Bayesian inference and online learning._
- **formalCalculus/radon-nikodym** declares `formalmlConnections` → `bayesian-inference` (connections)
  - Reciprocal field on the new topic: `formalcalculusPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Bayes' theorem is a change of measure: the posterior density is the prior times the likelihood ratio, which is a Radon-Nikodym derivative._

## When `formalml/bayesian-model-averaging` ships

Add the reciprocal pointer back to:

- **formalStatistics/model-selection-and-information-criteria** declares `formalmlConnections` → `bayesian-model-averaging` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _BIC approximates the log marginal likelihood of a single model. BMA weights predictions by posterior model probabilities. §24.4 Rem 7 flags; §24.10 Rem 23 forward-points; full treatment Track 7._

## When `formalml/bayesian-neural-networks` ships

Add the reciprocal pointer back to:

- **formalStatistics/bayesian-foundations-and-prior-selection** declares `formalmlConnections` → `bayesian-neural-networks` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _BNN posteriors are MVN near the MAP estimate by BvM (§25.8 Thm 5); the Laplace approximation (§25.8 Rem 16) is the canonical construction. formalml's BNN topic develops non-conjugate posterior appr…_
- **formalStatistics/central-limit-theorem** declares `formalmlConnections` → `bayesian-neural-networks` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _The Bernstein–von Mises theorem (the Bayesian CLT) proves that the posterior distribution √n(θ − θ₀) | X₁,…,Xₙ converges in total variation to N(0, I(θ₀)⁻¹), where I(θ₀) is the Fisher information. …_
- **formalStatistics/hierarchical-bayes-and-partial-pooling** declares `formalmlConnections` → `bayesian-neural-networks` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Hierarchical priors over weights, MC-dropout, deep ensembles, SG-MCMC over NN posteriors. §28.10 Rem 24 pointer._
- **formalStatistics/modes-of-convergence** declares `formalmlConnections` → `bayesian-neural-networks` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _The Bernstein–von Mises theorem states that the posterior distribution converges in total variation (hence in distribution) to a Normal centered at the MLE. This is the frequentist justification fo…_
- **formalStatistics/multivariate-distributions** declares `formalmlConnections` → `bayesian-neural-networks` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Weight priors w ~ N_p(0, σ²I) are multivariate Normal. The posterior p(w|D) is approximately MVN for well-specified models. The Laplace approximation uses the Hessian as the inverse covariance._

## When `formalml/bias-variance-tradeoff` ships

Add the reciprocal pointer back to:

- **formalStatistics/expectation-moments** declares `formalmlConnections` → `bias-variance-tradeoff` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _The bias-variance decomposition E[(Y−f̂)²] = Bias² + Variance + Noise is a direct application of Eve's law and the variance decomposition proved here._
- **formalStatistics/point-estimation** declares `formalmlConnections` → `bias-variance-tradeoff` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _The MSE decomposition Bias² + Variance is the formal version of the ML bias-variance tradeoff. Regularization, model selection, and ensemble methods all navigate this tradeoff._

## When `formalml/causal-inference` ships

Add the reciprocal pointer back to:

- **formalStatistics/confidence-intervals-and-duality** declares `formalmlConnections` → `causal-inference` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Instrumental-variables regression, doubly-robust estimation, and difference-in-differences all report confidence intervals on the average treatment effect. The Wald CI is most common; sandwich vari…_
- **formalStatistics/hypothesis-testing** declares `formalmlConnections` → `causal-inference` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Randomized experiments use t-tests and z-tests for average treatment effects; falsification and placebo tests are hypothesis tests for identification assumptions. The score test reappears as a spec…_

## When `formalml/causal-inference-methods` ships

Add the reciprocal pointer back to:

- **formalStatistics/empirical-processes** declares `formalmlConnections` → `causal-inference-methods` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Targeted maximum likelihood, augmented inverse-propensity weighting, and efficient-influence-function methods all rest on the functional delta method (§32.7) applied to nuisance-parameter empirical…_
- **formalStatistics/generalized-linear-models** declares `formalmlConnections` → `causal-inference-methods` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Inverse-probability-weighted estimators, doubly-robust estimators, and TMLE all use GLMs as the propensity / outcome model components. §22.10 Rem 27 catalog-points; the methodology lives at formalml._
- **formalStatistics/linear-regression** declares `formalmlConnections` → `causal-inference-methods` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Instrumental variables, difference-in-differences, regression discontinuity — all live in the linear-model framework where OLS + identifying restrictions give unbiased causal estimates. §21.10 Rem …_

## When `formalml/clustering` ships

Add the reciprocal pointer back to:

- **formalStatistics/kernel-density-estimation** declares `formalmlConnections` → `clustering` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Mean-shift clustering uses gradient ascent on $\hat f_h$ to find local modes — the KDE's stationary points *are* the cluster centers. §30.9 Rem 18 pointer; forthcoming on formalml._

## When `formalml/cross-entropy-loss` ships

Add the reciprocal pointer back to:

- **formalStatistics/maximum-likelihood** declares `formalmlConnections` → `cross-entropy-loss` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _MLE for a categorical model minimizes cross-entropy loss. The log-likelihood ℓ(θ) = Σ log f(xᵢ; θ) is (up to sign and an empirical-distribution constant) the negative cross-entropy between the empi…_

## When `formalml/cross-fitting` ships

Add the reciprocal pointer back to:

- **formalStatistics/model-selection-and-information-criteria** declares `formalmlConnections` → `cross-fitting` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Chernozhukov et al. 2018's double/debiased ML formalism uses cross-fitting for valid causal inference after ML-selected nuisance models. §24.10 Rem 24._

## When `formalml/cross-validation` ships

Add the reciprocal pointer back to:

- **formalStatistics/bootstrap** declares `formalmlConnections` → `cross-validation` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _§31.9's bias correction is the standard debiasing technique for cross-validated risk estimates — CV risk underestimates test risk by an amount that the bootstrap-bias estimator quantifies._

## When `formalml/density-ratio-estimation` ships

Add the reciprocal pointer back to:

- **formalStatistics/kernel-density-estimation** declares `formalmlConnections` → `density-ratio-estimation` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Sugiyama–Suzuki–Kanamori 2012 two-sample density-ratio estimation, the foundation of covariate-shift correction. §30.10 Rem 22 pointer; forthcoming on formalml._

## When `formalml/differential-privacy` ships

Add the reciprocal pointer back to:

- **formalStatistics/large-deviations** declares `formalmlConnections` → `differential-privacy` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _The Gaussian mechanism adds N(0, σ²) noise with σ calibrated via sub-Gaussian tail bounds: σ = Δ√(2 ln(1.25/δ))/ε. Concentrated differential privacy (zCDP) uses the sub-Gaussian MGF bound directly._

## When `formalml/diffusion-models` ships

Add the reciprocal pointer back to:

- **formalCalculus/radon-nikodym** declares `formalmlConnections` → `diffusion-models` (connections)
  - Reciprocal field on the new topic: `formalcalculusPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Score matching involves ∇ log p, the gradient of the log-Radon-Nikodym derivative. Diffusion model training minimizes an L²(p_t) distance involving scores._

## When `formalml/dimensionality-reduction` ships

Add the reciprocal pointer back to:

- **formalStatistics/large-deviations** declares `formalmlConnections` → `dimensionality-reduction` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _The Johnson–Lindenstrauss lemma proves that random projections from ℝᵈ to ℝᵏ with k = O(ε⁻² log n) approximately preserve pairwise distances among n points. The proof uses sub-Gaussian concentratio…_

## When `formalml/double-descent` ships

Add the reciprocal pointer back to:

- **formalStatistics/regularization-and-penalized-estimation** declares `formalmlConnections` → `double-descent` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Modern overparameterized regimes (neural networks, random-features models) violate Topic 23's $p < n$ assumption. The double-descent phenomenon — risk curve as a function of model complexity has a …_

## When `formalml/embedding-spaces` ships

Add the reciprocal pointer back to:

- **formalCalculus/metric-spaces** declares `formalmlConnections` → `embedding-spaces` (connections)
  - Reciprocal field on the new topic: `formalcalculusPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Word2Vec, BERT, and graph embeddings live in metric spaces where cosine or Euclidean distance measures semantic proximity. The triangle inequality constrains embedding geometry._

## When `formalml/empirical-risk-minimization` ships

Add the reciprocal pointer back to:

- **formalStatistics/law-of-large-numbers** declares `formalmlConnections` → `empirical-risk-minimization` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _ERM consistency — the minimizer of the empirical risk converges to the minimizer of the population risk — requires uniform convergence of R̂ₙ(θ) → R(θ) over θ, which is a generalization of the Gliv…_
- **formalStatistics/method-of-moments** declares `formalmlConnections` → `empirical-risk-minimization` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _M-estimation IS empirical risk minimization: θ̂ minimizes the empirical average of a loss ρ(X; θ), equivalently solves Σ ψ(X; θ) = 0 where ψ = ∇_θ ρ. Every ERM estimator with a smooth convex loss i…_

## When `formalml/ensemble-methods` ships

Add the reciprocal pointer back to:

- **formalStatistics/point-estimation** declares `formalmlConnections` → `ensemble-methods` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Bagging reduces variance (averaging independent estimators) without increasing bias. Boosting reduces bias by sequentially fitting residuals. Both strategies are applications of the MSE decomposition._

## When `formalml/expectation-maximization` ships

Add the reciprocal pointer back to:

- **formalStatistics/maximum-likelihood** declares `formalmlConnections` → `expectation-maximization` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _EM iteratively maximizes a lower bound on the log-likelihood for latent-variable models (mixtures, HMMs). Each M-step is an MLE of the complete-data log-likelihood._

## When `formalml/feature-engineering` ships

Add the reciprocal pointer back to:

- **formalStatistics/linear-regression** declares `formalmlConnections` → `feature-engineering` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Topic 21's scope boundary in §21.1 Rem 1 (linear in parameters, not in predictors) licenses polynomial basis expansions, interaction terms, and spline features — all of which preserve linearity in …_

## When `formalml/feature-selection-and-multiplicity` ships

Add the reciprocal pointer back to:

- **formalStatistics/multiple-testing-and-false-discovery** declares `formalmlConnections` → `feature-selection-and-multiplicity` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Every high-dimensional feature-selection procedure inherits a multiple-testing problem. BH is the default tool for selecting significant coefficients in deployed scikit-learn and R glmnet pipelines…_

## When `formalml/fourier-neural-operators` ships

Add the reciprocal pointer back to:

- **formalCalculus/lp-spaces** declares `formalmlConnections` → `fourier-neural-operators` (connections)
  - Reciprocal field on the new topic: `formalcalculusPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Fourier neural operators learn mappings between L²(ℝᵈ) spaces. The completeness (Riesz-Fischer) and inner product structure of L² are prerequisites for their well-definedness._

## When `formalml/gaussian-processes` ships

Add the reciprocal pointer back to:

- **formalStatistics/bayesian-foundations-and-prior-selection** declares `formalmlConnections` → `gaussian-processes` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _GPs are the infinite-dimensional generalization of Topic 25's finite-dimensional Bayesian framework: the prior is over functions rather than parameters, and the posterior is a conditional MVN (Topi…_
- **formalStatistics/continuous-distributions** declares `formalmlConnections` → `gaussian-processes` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _The Normal distribution is the marginal of every finite collection of GP outputs. The GP prior is an infinite-dimensional Gaussian._
- **formalStatistics/multivariate-distributions** declares `formalmlConnections` → `gaussian-processes` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _A GP defines a joint MVN over function values at any finite set of inputs. The conditional MVN formula from §4 IS the GP prediction formula — conditional mean = posterior mean, conditional covarian…_

## When `formalml/generalization-bounds` ships

Add the reciprocal pointer back to:

- **formalStatistics/central-limit-theorem** declares `formalmlConnections` → `generalization-bounds` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Rademacher complexity bounds use symmetrization and CLT-type reasoning. The empirical Rademacher complexity converges to its expectation at rate 1/√n, giving the O(1/√n) generalization bound for bo…_
- **formalStatistics/empirical-processes** declares `formalmlConnections` → `generalization-bounds` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _§32.4's G-C-for-VC bound is the empirical-process core of PAC generalization bounds; formalML develops the learning-theory framing with Rademacher complexity and uniform convergence of risk._
- **formalStatistics/large-deviations** declares `formalmlConnections` → `generalization-bounds` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _PAC learning bounds use Hoeffding: P(|R(ĥ) − R̂ₙ(ĥ)| > ε) ≤ 2|H| exp(−2nε²). Rademacher symmetrization uses McDiarmid's inequality. The O(1/√n) generalization rate for bounded loss classes originat…_
- **formalStatistics/law-of-large-numbers** declares `formalmlConnections` → `generalization-bounds` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _PAC learning bounds are WLLN statements: P(|R̂(h) − R(h)| > ε) ≤ δ. Uniform convergence over hypothesis classes (Vapnik–Chervonenkis theory) extends the Glivenko–Cantelli theorem from indicators to…_
- **formalStatistics/modes-of-convergence** declares `formalmlConnections` → `generalization-bounds` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _PAC learning bounds state that the empirical risk converges to the true risk in probability: P(|R̂(h) − R(h)| > ε) ≤ δ. The uniform convergence version (over hypothesis classes) uses the Glivenko–C…_

## When `formalml/generalized-linear-models` ships

Add the reciprocal pointer back to:

- **formalStatistics/confidence-intervals-and-duality** declares `formalmlConnections` → `generalized-linear-models` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _GLM coefficient inference uses the Wald/LRT CI trio of §19.4 at the scale of every deployed logistic, Poisson, and gamma regression. The LRT-based deviance-difference CI is the numerical default in…_
- **formalStatistics/continuous-distributions** declares `formalmlConnections` → `generalized-linear-models` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Normal response → linear regression (identity link). Gamma response → Gamma GLM (log link) for positive-valued outcomes like insurance claims and time durations._
- **formalStatistics/discrete-distributions** declares `formalmlConnections` → `generalized-linear-models` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Poisson regression (log link, count response) and Negative Binomial regression (overdispersed counts) use these distributions as the response family in GLMs._
- **formalStatistics/exponential-families** declares `formalmlConnections` → `generalized-linear-models` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Every GLM requires an exponential family response distribution. The canonical link function is (A')⁻¹. Logistic, Poisson, and Gamma regression are instances._
- **formalStatistics/generalized-linear-models** declares `formalmlConnections` → `generalized-linear-models` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _§22.10 Rem 26 forward-points: the ML-scale treatment covers multiclass softmax (multinomial GLM), the unifying information-geometry framing (natural-gradient = Fisher scoring = IRLS in continuous t…_
- **formalStatistics/likelihood-ratio-tests-and-np** declares `formalmlConnections` → `generalized-linear-models` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Wald/Score/LRT tests for GLM coefficients are the direct vector-θ generalization of §18.7. The score test's 'only fit the null model' advantage powers stepwise forward selection; the LRT-via-devian…_
- **formalStatistics/linear-regression** declares `formalmlConnections` → `generalized-linear-models` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Topic 22 is the immediate successor — logistic, Poisson, and gamma regression drop the Normal-errors assumption by composing a linear predictor with a link function and fitting via IRLS. The Wald /…_

## When `formalml/generalized-method-of-moments` ships

Add the reciprocal pointer back to:

- **formalStatistics/method-of-moments** declares `formalmlConnections` → `generalized-method-of-moments` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _GMM (Hansen 1982) generalizes MoM to over-identified systems — more moment equations than parameters — solved by weighted least squares on the moment residuals. Previewed in §15.12 as the forward l…_

## When `formalml/generative-modeling` ships

Add the reciprocal pointer back to:

- **formalCalculus/banach-spaces** declares `formalmlConnections` → `generative-modeling` (connections)
  - Reciprocal field on the new topic: `formalcalculusPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Spectral normalization constrains neural network weight matrices to have operator norm at most 1, enforcing Lipschitz continuity for stable Wasserstein GAN training._
- **formalCalculus/calculus-of-variations** declares `formalmlConnections` → `generative-modeling` (connections)
  - Reciprocal field on the new topic: `formalcalculusPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Variational autoencoders minimize the ELBO — a variational objective over encoder-decoder function pairs. Diffusion models minimize score-matching losses with variational structure._
- **formalCalculus/hilbert-spaces** declares `formalmlConnections` → `generative-modeling` (connections)
  - Reciprocal field on the new topic: `formalcalculusPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Gaussian processes operate in RKHS. The posterior mean is a projection onto a finite-dimensional subspace spanned by the kernel evaluations at observed data points._
- **formalCalculus/radon-nikodym** declares `formalmlConnections` → `generative-modeling` (connections)
  - Reciprocal field on the new topic: `formalcalculusPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Normalizing flows use the change-of-variables formula p_Y(y) = p_X(T⁻¹(y)) · |det DT⁻¹|, which is a Radon-Nikodym derivative under pushforward._

## When `formalml/graphical-models` ships

Add the reciprocal pointer back to:

- **formalStatistics/conditional-probability** declares `formalmlConnections` → `graphical-models` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Conditional independence is the structural assumption encoded in Bayesian networks. The factorization P(X₁,..., Xₙ) = ∏ P(Xᵢ | Parents(Xᵢ)) is the chain rule of probability combined with conditiona…_

## When `formalml/high-dimensional-regression` ships

Add the reciprocal pointer back to:

- **formalStatistics/generalized-linear-models** declares `formalmlConnections` → `high-dimensional-regression` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _When $p \gg n$ the GLM design matrix is rank-deficient and ordinary IRLS fails. §22.10 Rem 28 forward-points to the high-dimensional treatment: penalized GLMs ([Topic 23](/topics/regularization-and…_
- **formalStatistics/linear-regression** declares `formalmlConnections` → `high-dimensional-regression` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _When $p > n$, $\mathbf{X}^\top \mathbf{X}$ is singular and OLS fails. [Topic 23](/topics/regularization-and-penalized-estimation) (ridge, lasso) is the Track-6 answer; the high-dimensional theory (…_
- **formalStatistics/model-selection-and-information-criteria** declares `formalmlConnections` → `high-dimensional-regression` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _When $p \gg n$, likelihood-based IC degenerates. Extended BIC (Chen–Chen 2008), stability selection (Meinshausen–Bühlmann 2010), and knockoffs (Barber–Candès 2015) extend the framework. §24.10 Rem 25._
- **formalStatistics/regularization-and-penalized-estimation** declares `formalmlConnections` → `high-dimensional-regression` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _§23.10 Rem 29 forward-points to the asymptotic theory under $p \gg n$: oracle inequalities, restricted-eigenvalue conditions (Bickel–Ritov–Tsybakov 2009), the lasso $\ell_2$-consistency rate (Wainw…_

## When `formalml/high-dimensional-testing-knockoffs` ships

Add the reciprocal pointer back to:

- **formalStatistics/multiple-testing-and-false-discovery** declares `formalmlConnections` → `high-dimensional-testing-knockoffs` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _The Barber–Candès (2015) knockoffs framework is a direct descendent of BH: it constructs synthetic 'knockoff' features, computes test statistics per feature, and applies a BH-like step-up. The $\al…_

## When `formalml/importance-sampling` ships

Add the reciprocal pointer back to:

- **formalCalculus/radon-nikodym** declares `formalmlConnections` → `importance-sampling` (connections)
  - Reciprocal field on the new topic: `formalcalculusPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _The importance weight dP/dQ is literally a Radon-Nikodym derivative. The identity E_P[f] = E_Q[f · dP/dQ] is a change of measure._

## When `formalml/information-bottleneck` ships

Add the reciprocal pointer back to:

- **formalStatistics/sufficient-statistics** declares `formalmlConnections` → `information-bottleneck` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _The information bottleneck method finds a compressed representation $T$ of the data $X$ that preserves information about a target $Y$. In the special case $\theta = Y$ and $T$ is the sufficient sta…_

## When `formalml/information-theory` ships

Add the reciprocal pointer back to:

- **formalStatistics/expectation-moments** declares `formalmlConnections` → `information-theory` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Cross-entropy H(p,q) = −E_p[log q] and KL divergence D_KL(p||q) = E_p[log(p/q)] are expectations. Jensen's inequality immediately gives D_KL ≥ 0._

## When `formalml/kernel-methods` ships

Add the reciprocal pointer back to:

- **formalCalculus/banach-spaces** declares `formalmlConnections` → `kernel-methods` (connections)
  - Reciprocal field on the new topic: `formalcalculusPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _RKHS is a Hilbert space (hence Banach). The representer theorem exploits dual-space structure: bounded linear functionals on RKHS correspond to kernel evaluations via the Riesz representation theor…_
- **formalCalculus/hilbert-spaces** declares `formalmlConnections` → `kernel-methods` (connections)
  - Reciprocal field on the new topic: `formalcalculusPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _RKHS is a Hilbert space; the representer theorem is a consequence of the Riesz representation theorem. Kernel evaluation is inner-product evaluation via the reproducing property._
- **formalCalculus/lp-spaces** declares `formalmlConnections` → `kernel-methods` (connections)
  - Reciprocal field on the new topic: `formalcalculusPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Reproducing kernel Hilbert spaces (RKHS) are subspaces of L². Kernel evaluations are inner products in L², and the representer theorem uses L² completeness._
- **formalCalculus/metric-spaces** declares `formalmlConnections` → `kernel-methods` (connections)
  - Reciprocal field on the new topic: `formalcalculusPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Positive-definite kernels induce metrics on feature spaces. The RKHS norm distance is a metric-space distance, and kernel methods exploit metric-space completeness._

## When `formalml/kernel-regression` ships

Add the reciprocal pointer back to:

- **formalStatistics/kernel-density-estimation** declares `formalmlConnections` → `kernel-regression` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Nadaraya–Watson kernel regression is the covariate-conditional extension of KDE: average $Y_i$ values weighted by $K_h(x - X_i)$. §30.9 Rem 17 pointer; full development forthcoming on formalml._

## When `formalml/local-regression` ships

Add the reciprocal pointer back to:

- **formalStatistics/kernel-density-estimation** declares `formalmlConnections` → `local-regression` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Fan–Gijbels 1996 local polynomial regression corrects KDE's boundary bias and generalizes to the regression setting. §30.5 Rem 9 name-checks the local-linear-at-boundary estimator; forthcoming on f…_

## When `formalml/logistic-regression` ships

Add the reciprocal pointer back to:

- **formalStatistics/discrete-distributions** declares `formalmlConnections` → `logistic-regression` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Logistic regression models Y|X ~ Bernoulli(σ(β^T X)). The natural parameter η = log(p/(1−p)) = β^T X is the log-odds, directly from the exponential family form. Cross-entropy loss is the negative B…_
- **formalStatistics/generalized-linear-models** declares `formalmlConnections` → `logistic-regression` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _§22.4 Rem 9 forward-points: cross-entropy loss in deep learning is literally the negative Bernoulli log-likelihood per observation, and the final-layer sigmoid is a logistic GLM. Every binary class…_
- **formalStatistics/maximum-likelihood** declares `formalmlConnections` → `logistic-regression` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Logistic regression is MLE for a Bernoulli GLM with logit link. The log-likelihood has no closed-form MLE; Newton-Raphson (IRLS) computes it iteratively._

## When `formalml/meta-learning` ships

Add the reciprocal pointer back to:

- **formalStatistics/hierarchical-bayes-and-partial-pooling** declares `formalmlConnections` → `meta-learning` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _MAML and hierarchical Bayes over task distributions; the machine-learning reading of §28's group-level hyperparameters. §28.10 Ex 14 pointer._

## When `formalml/metric-learning` ships

Add the reciprocal pointer back to:

- **formalCalculus/metric-spaces** declares `formalmlConnections` → `metric-learning` (connections)
  - Reciprocal field on the new topic: `formalcalculusPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Metric spaces formalize learned similarity. Mahalanobis distance, Siamese networks, and triplet loss all learn metrics on embedding spaces — the axioms here are exactly the constraints those losses…_

## When `formalml/mixed-effects` ships

Add the reciprocal pointer back to:

- **formalStatistics/generalized-linear-models** declares `formalmlConnections` → `mixed-effects` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _§22.10 Rem 25 forward-points: GLMMs add random effects $\mathbf{b} \sim \mathcal{N}(\mathbf{0}, \boldsymbol\Sigma)$ to the linear predictor for clustered or hierarchical data. Topic 22 covers the f…_
- **formalStatistics/hierarchical-bayes-and-partial-pooling** declares `formalmlConnections` → `mixed-effects` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _REML, lme4, Stan-style GLMMs, recommender-system random-effects structures. §28.8 Rem 20 pointer._

## When `formalml/model-comparison` ships

Add the reciprocal pointer back to:

- **formalStatistics/hypothesis-testing** declares `formalmlConnections` → `model-comparison` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Cross-validation, AIC, BIC, and held-out likelihood are ML-native alternatives to nested hypothesis testing. §17.11 Remark 14 frames the conceptual contrast: classical testing asks 'reject $H_0$?';…_
- **formalStatistics/likelihood-ratio-tests-and-np** declares `formalmlConnections` → `model-comparison` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Wilks' theorem is the frequentist backbone of the likelihood-ratio test for nested models — the answer to 'did the extra parameters help?' The Bayesian analog is the Bayes factor ([Topic 27 §27.4](…_

## When `formalml/monte-carlo-methods` ships

Add the reciprocal pointer back to:

- **formalStatistics/central-limit-theorem** declares `formalmlConnections` → `monte-carlo-methods` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _The CLT gives the convergence rate of Monte Carlo estimators: the standard error of (1/n)Σf(Xᵢ) is σ_f/√n, leading to the canonical ±1.96σ/√n confidence band for Monte Carlo estimates._
- **formalStatistics/large-deviations** declares `formalmlConnections` → `monte-carlo-methods` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Hoeffding gives non-asymptotic confidence bands for bounded Monte Carlo estimators: n ≥ (b−a)² ln(2/δ)/(2ε²) samples suffice for ε-accuracy with probability 1 − δ. This complements the CLT's asympt…_
- **formalStatistics/law-of-large-numbers** declares `formalmlConnections` → `monte-carlo-methods` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _The SLLN is the theoretical foundation of all Monte Carlo methods. [Topic 26 §26.7 Theorem 5](/topics/bayesian-computation-and-mcmc#section-26-7) proves the Markov-chain ergodic theorem — the gener…_

## When `formalml/naive-bayes` ships

Add the reciprocal pointer back to:

- **formalStatistics/discrete-distributions** declares `formalmlConnections` → `naive-bayes` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Bernoulli Naive Bayes models binary feature vectors as independent Bernoulli random variables given the class label._

## When `formalml/normalizing-flows` ships

Add the reciprocal pointer back to:

- **formalStatistics/continuous-distributions** declares `formalmlConnections` → `normalizing-flows` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _The standard Normal N(0,I) is the base distribution. The change-of-variables formula transforms it through invertible mappings to model complex densities._
- **formalStatistics/kernel-density-estimation** declares `formalmlConnections` → `normalizing-flows` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Rezende–Mohamed 2015 normalizing flows replace the kernel-average-of-bumps with a learned invertible transformation of a simple base density. §30.10 Rem 20 pointer; orthogonal to classical AMISE th…_
- **formalStatistics/multivariate-distributions** declares `formalmlConnections` → `normalizing-flows` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Normalizing flows learn an invertible transform g from N(0,I) to a complex target. The change of variables formula f_Y(y) = f_Z(g^{-1}(y)) |det J| from §8 is the core mathematical tool._
- **formalStatistics/random-variables** declares `formalmlConnections` → `normalizing-flows` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Normalizing flows learn invertible transformations of simple distributions. The change of variables formula f_Y(y) = f_X(g⁻¹(y)) · |det J_{g⁻¹}(y)| is the mathematical backbone — each flow layer ap…_
- **formalCalculus/sigma-algebras** declares `formalmlConnections` → `normalizing-flows` (connections)
  - Reciprocal field on the new topic: `formalcalculusPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Normalizing flows are pushforward measures: T*μ_X = μ_Y. The change-of-variables formula for densities is a Radon-Nikodym derivative of the pushforward._

## When `formalml/online-fdr` ships

Add the reciprocal pointer back to:

- **formalStatistics/multiple-testing-and-false-discovery** declares `formalmlConnections` → `online-fdr` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _The streaming generalization of BH — Javanmard–Montanari 2018 Online-BH and Ramdas et al. 2018 SAFFRON / LORD — allows hypotheses to arrive sequentially with FDR control at each time. §20.10 Rem 27…_

## When `formalml/optimization` ships

Add the reciprocal pointer back to:

- **formalStatistics/expectation-moments** declares `formalmlConnections` → `optimization` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _The training objective Risk(f) = E[ℓ(Y, f(X))] is an expectation over the data distribution. Stochastic gradient descent estimates ∇E[L] with ∇L on minibatches — linearity of expectation justifies …_

## When `formalml/optimization-theory` ships

Add the reciprocal pointer back to:

- **formalCalculus/banach-spaces** declares `formalmlConnections` → `optimization-theory` (connections)
  - Reciprocal field on the new topic: `formalcalculusPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Mirror descent operates in Banach spaces with Bregman divergences. The dual-space geometry — Fenchel conjugates, subgradients in X* — extends gradient descent beyond Hilbert spaces._
- **formalCalculus/hilbert-spaces** declares `formalmlConnections` → `optimization-theory` (connections)
  - Reciprocal field on the new topic: `formalcalculusPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Hilbert-space gradient descent converges because the Riesz identification turns dual-space gradients into primal-space descent directions — no dual-space bookkeeping needed._

## When `formalml/pac-bayes-bounds` ships

Add the reciprocal pointer back to:

- **formalStatistics/empirical-processes** declares `formalmlConnections` → `pac-bayes-bounds` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _PAC-Bayes lifts Topic 32's function-class complexity arguments to a posterior-over-hypotheses setting; McAllester's 1999 bound uses the same symmetrization technique §32.4 introduces._

## When `formalml/post-selection-inference` ships

Add the reciprocal pointer back to:

- **formalStatistics/model-selection-and-information-criteria** declares `formalmlConnections` → `post-selection-inference` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _After AIC/BIC/CV selection, naive Wald CIs are invalid because the selection event is data-dependent. Debiased lasso (Zhang–Zhang 2014), selective conditioning (Lee–Sun–Sun–Taylor 2016), and cross-…_

## When `formalml/principal-component-analysis` ships

Add the reciprocal pointer back to:

- **formalStatistics/multivariate-distributions** declares `formalmlConnections` → `principal-component-analysis` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _PCA is the eigendecomposition of the sample covariance matrix. The eigenvectors are the principal directions, and the eigenvalues are the explained variances. The MVN geometry from §7 makes this co…_

## When `formalml/probabilistic-programming` ships

Add the reciprocal pointer back to:

- **formalStatistics/bayesian-computation-and-mcmc** declares `formalmlConnections` → `probabilistic-programming` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Stan, PyMC, NumPyro — domain-specific languages that compile user-specified models into NUTS samplers. Topic 26 motivates; formalml explores the compiler stack. §26.10 Rem 33._
- **formalStatistics/bayesian-foundations-and-prior-selection** declares `formalmlConnections` → `probabilistic-programming` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Probabilistic programming languages (Stan, PyMC, NumPyro) automate the posterior computation Topic 25 does by hand — users specify prior × likelihood, the language handles sampling via HMC/NUTS. To…_

## When `formalml/probability-spaces` ships

Add the reciprocal pointer back to:

- **formalCalculus/lebesgue-integral** declares `formalmlConnections` → `probability-spaces` (connections)
  - Reciprocal field on the new topic: `formalcalculusPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Expected value E[X] = ∫ X dP is a Lebesgue integral against a probability measure. Every moment, MGF, and characteristic function computation is an application of this topic._
- **formalCalculus/radon-nikodym** declares `formalmlConnections` → `probability-spaces` (connections)
  - Reciprocal field on the new topic: `formalcalculusPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Probability densities are Radon-Nikodym derivatives dP/dλ. The entire measure-theoretic probability framework rests on the R-N theorem, making densities rigorous._
- **formalCalculus/sigma-algebras** declares `formalmlConnections` → `probability-spaces` (connections)
  - Reciprocal field on the new topic: `formalcalculusPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _A probability space (Ω, F, P) is a measure space with P(Ω) = 1. Sigma-algebras formalize observable events; filtrations model information flow._

## When `formalml/regularization` ships

Add the reciprocal pointer back to:

- **formalStatistics/maximum-likelihood** declares `formalmlConnections` → `regularization` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _MAP estimation adds a prior log-density to the log-likelihood: log L(θ) + log π(θ). Gaussian prior → L² penalty; Laplace prior → L¹ penalty. Regularized MLE is the Bayesian bridge._
- **formalStatistics/point-estimation** declares `formalmlConnections` → `regularization` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _L2 regularization (ridge regression) deliberately introduces bias to reduce variance — a James-Stein-type shrinkage. The optimal regularization parameter minimizes MSE, not bias alone._
- **formalStatistics/regularization-and-penalized-estimation** declares `formalmlConnections` → `regularization` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _§23.10 Rem 28 forward-points to the ML-scale treatment of regularization including weight decay, dropout (as implicit regularization), data augmentation, label smoothing, and early stopping. The pe…_

## When `formalml/reinforcement-learning` ships

Add the reciprocal pointer back to:

- **formalCalculus/metric-spaces** declares `formalmlConnections` → `reinforcement-learning` (connections)
  - Reciprocal field on the new topic: `formalcalculusPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _The Bellman operator is a contraction in the sup-norm on bounded value functions. Banach's fixed-point theorem guarantees that value iteration converges to the unique optimal value function V*._
- **formalStatistics/modes-of-convergence** declares `formalmlConnections` → `reinforcement-learning` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Value iteration and Q-learning produce sequences of value functions that converge almost surely to the optimal value function under appropriate step-size schedules — the same Robbins–Monro machiner…_

## When `formalml/representation-learning` ships

Add the reciprocal pointer back to:

- **formalStatistics/sufficient-statistics** declares `formalmlConnections` → `representation-learning` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Self-supervised learning objectives (contrastive, masked-modeling) implicitly discover sufficient statistics of $X$ for downstream tasks. The Rao-Blackwellization intuition — that optimal estimator…_

## When `formalml/reversible-jump-mcmc` ships

Add the reciprocal pointer back to:

- **formalStatistics/bayesian-model-comparison-and-bma** declares `formalmlConnections` → `reversible-jump-mcmc` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _RJMCMC (Green 1995) samples directly from the joint posterior over $(\mathcal{M}_k, \theta_k)$, providing BMA weights as a byproduct. An alternative to separate marginal-likelihood estimates when t…_

## When `formalml/robust-statistics` ships

Add the reciprocal pointer back to:

- **formalStatistics/method-of-moments** declares `formalmlConnections` → `robust-statistics` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Huber and Tukey M-estimators are the foundation of modern robust regression. The breakdown point and influence function quantify resistance to outliers. Huber loss is used routinely in ML for regre…_

## When `formalml/score-matching` ships

Add the reciprocal pointer back to:

- **formalCalculus/lp-spaces** declares `formalmlConnections` → `score-matching` (connections)
  - Reciprocal field on the new topic: `formalcalculusPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Score matching minimizes ∫||∇ log p_θ - ∇ log p_data||² dx, an L² norm of gradient differences. Well-definedness requires ∇ log p ∈ L²._

## When `formalml/semiparametric-inference` ships

Add the reciprocal pointer back to:

- **formalStatistics/empirical-processes** declares `formalmlConnections` → `semiparametric-inference` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _M-estimator asymptotics — the modern nonparametric-plus-parametric-component framework — is built on local empirical-process theory (local Donsker, local bracketing), which §32.10 Rem 22 defers._

## When `formalml/sequential-monte-carlo` ships

Add the reciprocal pointer back to:

- **formalStatistics/bayesian-computation-and-mcmc** declares `formalmlConnections` → `sequential-monte-carlo` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _SMC / particle filters are the alternative when the target is a sequence π_t indexed by time or annealing parameter. Parallel to MCMC's single-target framework. §26.10 Rem 31._

## When `formalml/sparse-bayesian-priors` ships

Add the reciprocal pointer back to:

- **formalStatistics/hierarchical-bayes-and-partial-pooling** declares `formalmlConnections` → `sparse-bayesian-priors` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _The horseshoe, regularized horseshoe, spike-and-slab, and R2-D2 priors are the continuous-shrinkage alternatives to the hierarchical-Normal prior that §28.6 develops. §28.10 Rem 23 pointer._

## When `formalml/spectral-methods` ships

Add the reciprocal pointer back to:

- **formalCalculus/hilbert-spaces** declares `formalmlConnections` → `spectral-methods` (connections)
  - Reciprocal field on the new topic: `formalcalculusPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _The spectral theorem for compact self-adjoint operators is the foundation of PCA, kernel PCA, and spectral clustering._

## When `formalml/stacking-and-predictive-ensembles` ships

Add the reciprocal pointer back to:

- **formalStatistics/bayesian-model-comparison-and-bma** declares `formalmlConnections` → `stacking-and-predictive-ensembles` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Stacking is the M-open alternative to BMA: optimize predictive weights directly rather than derive them from marginal likelihoods. Critical when no candidate model is true. §27.10 Rem 26 sets up th…_

## When `formalml/statistical-learning-theory` ships

Add the reciprocal pointer back to:

- **formalStatistics/confidence-intervals-and-duality** declares `formalmlConnections` → `statistical-learning-theory` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _PAC-Bayes generalization bounds are uniform (1−δ) confidence statements on the hypothesis class. The coverage-calibration theme of §19.8 generalizes to PAC-bound tightness diagnostics in the learni…_
- **formalStatistics/hypothesis-testing** declares `formalmlConnections` → `statistical-learning-theory` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _PAC generalization bounds are uniform-confidence statements of the form $P(\sup_h |\hat R(h) - R(h)| > \epsilon) \le \delta$ — Type-I-error-style guarantees. VC bounds and Rademacher complexity pro…_
- **formalStatistics/likelihood-ratio-tests-and-np** declares `formalmlConnections` → `statistical-learning-theory` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _PAC-Bayes and VC-dimension generalization bounds are uniform-concentration statements whose proofs mirror the indicator-function comparison of Proof 1 (Neyman-Pearson). The 'MP test = LR test' dual…_

## When `formalml/stochastic-gradient-mcmc` ships

Add the reciprocal pointer back to:

- **formalStatistics/bayesian-computation-and-mcmc** declares `formalmlConnections` → `stochastic-gradient-mcmc` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _SGLD (Welling–Teh 2011) and SGHMC (Chen–Fox–Guestrin 2014) replace the full-batch gradient in HMC / Langevin with a mini-batch gradient plus injected noise. Scales MCMC to large-n datasets at the c…_

## When `formalml/structural-risk-minimization` ships

Add the reciprocal pointer back to:

- **formalStatistics/model-selection-and-information-criteria** declares `formalmlConnections` → `structural-risk-minimization` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _Vapnik's SRM generalizes information criteria to function classes via VC dimension or Rademacher complexity. §24.10 Rem 26._

## When `formalml/uncertainty-quantification` ships

Add the reciprocal pointer back to:

- **formalStatistics/bootstrap** declares `formalmlConnections` → `uncertainty-quantification` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _§31.3's Theorem 3 underwrites bootstrapped model uncertainty: bag multiple bootstrap replicates of the training set, refit the model on each, and use the distribution of test-time predictions as a …_

## When `formalml/variational-bayes-for-model-selection` ships

Add the reciprocal pointer back to:

- **formalStatistics/bayesian-model-comparison-and-bma** declares `formalmlConnections` → `variational-bayes-for-model-selection` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _The evidence lower bound (ELBO) is a variational approximation to $\log m(\mathbf{y})$ derived from the Kullback–Leibler projection of the posterior onto a tractable family. Replaces MCMC+bridge fo…_

## When `formalml/wasserstein-distances` ships

Add the reciprocal pointer back to:

- **formalCalculus/lp-spaces** declares `formalmlConnections` → `wasserstein-distances` (connections)
  - Reciprocal field on the new topic: `formalcalculusPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _The Kantorovich dual formulation of Wasserstein-p distances uses (Lp)* = Lq duality. The constraint set is described using Lp norms._

## When `formalml/weight-decay` ships

Add the reciprocal pointer back to:

- **formalStatistics/regularization-and-penalized-estimation** declares `formalmlConnections` → `weight-decay` (connections)
  - Reciprocal field on the new topic: `formalstatisticsPrereqs`
  - Source-side prose (use as a starting point, rewrite from new topic's vantage): _§23.7 Rem 18 forward-points: weight decay in stochastic gradient descent and AdamW (decoupled weight decay) are MAP estimation with a Gaussian prior, exactly as Thm 5 says. The implementation diffe…_
